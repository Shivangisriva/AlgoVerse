# Write your MySQL query statement below
Select query_name , round(avg(rating/position),2) as quality , 
round(avg(Case when rating<3 then 1 else 0 end)*100,2) as poor_query_paercentage 
from
Queries
group by query_name
