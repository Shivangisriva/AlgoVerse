 public int maximalRectangle(char[][] matrix) {
        int n = matrix.length;
        int m = matrix[0].length;
     int pre[] = new int[m];
        int sum =0;
        int maxArea = 0;
        for(int i = 0 ; i<n;i++)
        {
            sum = 0;
    for(int j = 0 ; j<m;j++)
    {
     if (matrix[i][j] == '1') {
                    pre[j] += 1;
                } else {
                    pre[j] = 0;
                }
            }
            // Largest Rectangle in Histogram for this row
            maxArea = Math.max(maxArea, largestRecInHisto(pre));
        }
        return maxArea;
}
}
