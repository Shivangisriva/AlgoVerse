class Solution {
    public int countFriendsPairings(int n) {
        // code here
        if(n<=2)
        return n;
        return countFriendsPairings(n-1) + (n-1)*countFriendsPairings(n-2);
    }
}

/*
if we pair as a single friend we have n-1 choices left for the others = f(n-1)
if we pair in two then the no. of choices to pair friend are with (n-1) pairsa  and remaining is f(n-2) choices left
